<!DOCTYPE html>
<html lang="pt-BR" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Configurar MFA - Meu Portfólio</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/styles.css">
    <script>
        if (localStorage.theme === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
            document.documentElement.classList.add('dark');
        } else {
            document.documentElement.classList.remove('dark');
        }
    </script>
</head>
<body class="bg-gradient-to-br from-blue-50 to-indigo-100 dark:from-gray-900 dark:to-gray-800 min-h-screen flex items-center justify-center">
    
    <div class="w-full max-w-2xl mx-auto p-6">
        
        <!-- Card Principal -->
        <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-2xl overflow-hidden">
            
            <!-- Header -->
            <div class="bg-gradient-to-r from-blue-500 to-indigo-600 px-8 py-6 text-center">
                <h1 class="text-2xl font-bold text-white mb-2">🔐 Configurar Autenticação de Dois Fatores</h1>
                <p class="text-blue-100 text-sm">Aumente a segurança da sua conta de administrador</p>
            </div>
            
            <!-- Conteúdo -->
            <div class="px-8 py-8">
                
                <!-- Explicação -->
                <div class="mb-8 p-6 bg-blue-50 dark:bg-blue-900/20 rounded-lg border border-blue-200 dark:border-blue-700">
                    <h3 class="text-lg font-bold text-blue-800 dark:text-blue-200 mb-3">
                        🛡️ O que é a Autenticação de Dois Fatores (MFA)?
                    </h3>
                    <p class="text-blue-700 dark:text-blue-300 text-sm leading-relaxed">
                        A MFA adiciona uma camada extra de segurança à sua conta. Além da senha, você precisará 
                        de um código temporário gerado por um aplicativo como Google Authenticator, Authy ou Microsoft Authenticator.
                    </p>
                </div>
                
                <!-- Passo 1: App Authenticator -->
                <div class="mb-8">
                    <h3 class="text-lg font-bold text-gray-800 dark:text-gray-200 mb-4">
                        📱 Passo 1: Instale um aplicativo autenticador
                    </h3>
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                        <div class="p-4 border border-gray-200 dark:border-gray-600 rounded-lg text-center">
                            <div class="text-2xl mb-2">📱</div>
                            <h4 class="font-bold text-sm">Google Authenticator</h4>
                            <p class="text-xs text-gray-600 dark:text-gray-400">iOS & Android</p>
                        </div>
                        <div class="p-4 border border-gray-200 dark:border-gray-600 rounded-lg text-center">
                            <div class="text-2xl mb-2">🔐</div>
                            <h4 class="font-bold text-sm">Authy</h4>
                            <p class="text-xs text-gray-600 dark:text-gray-400">iOS & Android</p>
                        </div>
                        <div class="p-4 border border-gray-200 dark:border-gray-600 rounded-lg text-center">
                            <div class="text-2xl mb-2">🔒</div>
                            <h4 class="font-bold text-sm">Microsoft Authenticator</h4>
                            <p class="text-xs text-gray-600 dark:text-gray-400">iOS & Android</p>
                        </div>
                    </div>
                </div>
                
                <!-- Passo 2: QR Code -->
                <div class="mb-8">
                    <h3 class="text-lg font-bold text-gray-800 dark:text-gray-200 mb-4">
                        📷 Passo 2: Escaneie o QR Code
                    </h3>
                    
                    <div class="flex flex-col md:flex-row gap-8 items-center">
                        <!-- QR Code -->
                        <div class="text-center">
                            <div class="bg-white p-4 rounded-lg border border-gray-200 inline-block">
                                <img src="<%= qrCode %>" alt="QR Code MFA" class="w-48 h-48">
                            </div>
                            <p class="text-sm text-gray-600 dark:text-gray-400 mt-2">
                                Escaneie com seu app autenticador
                            </p>
                        </div>
                        
                        <!-- OU -->
                        <div class="text-center text-gray-500 dark:text-gray-400 font-bold">
                            OU
                        </div>
                        
                        <!-- Entrada manual -->
                        <div class="flex-1">
                            <h4 class="font-bold text-gray-800 dark:text-gray-200 mb-3">
                                ⌨️ Configuração Manual
                            </h4>
                            <p class="text-sm text-gray-600 dark:text-gray-400 mb-3">
                                Se não conseguir escanear, digite esta chave manualmente:
                            </p>
                            <div class="bg-gray-100 dark:bg-gray-700 p-3 rounded border font-mono text-sm break-all">
                                <%= manualEntryKey %>
                            </div>
                            <button onclick="copyToClipboard()" class="mt-2 text-blue-600 dark:text-blue-400 text-sm hover:text-blue-800">
                                📋 Copiar chave
                            </button>
                        </div>
                    </div>
                </div>
                
                <!-- Passo 3: Verificação -->
                <div class="mb-8">
                    <h3 class="text-lg font-bold text-gray-800 dark:text-gray-200 mb-4">
                        ✅ Passo 3: Confirme a configuração
                    </h3>
                    
                    <form action="/mfa-setup" method="POST" class="space-y-4">
                        <input type="hidden" name="secret" value="<%= secret %>">
                        
                        <div>
                            <label for="token" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
                                Digite o código de 6 dígitos do seu aplicativo:
                            </label>
                            <input 
                                type="text" 
                                id="token" 
                                name="token" 
                                required 
                                maxlength="6"
                                pattern="[0-9]{6}"
                                class="w-full px-4 py-3 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent dark:bg-gray-700 dark:text-white text-center text-2xl font-mono"
                                placeholder="000000"
                            >
                        </div>
                        
                        <div class="flex space-x-4">
                            <button 
                                type="submit"
                                class="flex-1 bg-gradient-to-r from-blue-500 to-indigo-600 text-white py-3 px-6 rounded-lg font-bold hover:from-blue-600 hover:to-indigo-700 transform transition duration-200 hover:scale-105 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2"
                            >
                                🔐 Ativar MFA
                            </button>
                            
                            <a 
                                href="/dashboard" 
                                class="flex-1 bg-gray-500 text-white py-3 px-6 rounded-lg font-bold hover:bg-gray-600 transform transition duration-200 hover:scale-105 focus:outline-none focus:ring-2 focus:ring-gray-500 focus:ring-offset-2 text-center"
                            >
                                ❌ Cancelar
                            </a>
                        </div>
                    </form>
                </div>
                
                <!-- Aviso de Segurança -->
                <div class="p-4 bg-yellow-50 dark:bg-yellow-900/20 border border-yellow-200 dark:border-yellow-700 rounded-lg">
                    <h4 class="font-bold text-yellow-800 dark:text-yellow-200 mb-2">
                        ⚠️ Importante:
                    </h4>
                    <ul class="text-yellow-700 dark:text-yellow-300 text-sm space-y-1">
                        <li>• Mantenha seu aplicativo autenticador seguro</li>
                        <li>• Guarde os códigos de backup em local seguro</li>
                        <li>• Não compartilhe os códigos com ninguém</li>
                        <li>• Se perder acesso, use os códigos de backup</li>
                    </ul>
                </div>
                
            </div>
            
        </div>
        
    </div>
    
    <script>
        function copyToClipboard() {
            const key = '<%= manualEntryKey %>';
            navigator.clipboard.writeText(key).then(() => {
                alert('Chave copiada para a área de transferência!');
            });
        }
        
        // Auto-format do código (adicionar espaços)
        document.getElementById('token').addEventListener('input', function(e) {
            let value = e.target.value.replace(/\D/g, '');
            if (value.length > 6) value = value.substring(0, 6);
            e.target.value = value;
        });
    </script>
    
    <script src="/js/theme-toggle.js"></script>
</body>
</html>
