<!DOCTYPE html>
<html lang="pt-BR" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verificação MFA - Meu Portfólio</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/styles.css">
    <script>
        if (localStorage.theme === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
            document.documentElement.classList.add('dark');
        } else {
            document.documentElement.classList.remove('dark');
        }
    </script>
</head>
<body class="bg-gradient-to-br from-blue-50 to-indigo-100 dark:from-gray-900 dark:to-gray-800 min-h-screen flex items-center justify-center">
    
    <div class="w-full max-w-md mx-auto p-6">
        
        <!-- Card Principal -->
        <div class="bg-white dark:bg-gray-800 rounded-2xl shadow-2xl overflow-hidden">
            
            <!-- Header -->
            <div class="bg-gradient-to-r from-blue-500 to-indigo-600 px-8 py-6 text-center">
                <h1 class="text-2xl font-bold text-white mb-2">🔐 Verificação de Segurança</h1>
                <p class="text-blue-100 text-sm">Digite o código do seu aplicativo autenticador</p>
            </div>
            
            <!-- Conteúdo -->
            <div class="px-8 py-8">
                
                <!-- Usuário -->
                <div class="text-center mb-6">
                    <div class="w-16 h-16 bg-blue-100 rounded-full flex items-center justify-center mx-auto mb-3">
                        <svg class="w-8 h-8 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
                        </svg>
                    </div>
                    <h2 class="text-lg font-bold text-gray-800 dark:text-gray-200">
                        Olá, <span class="text-blue-600"><%= username %></span>!
                    </h2>
                    <p class="text-gray-600 dark:text-gray-400 text-sm">
                        Por favor, complete a verificação de dois fatores
                    </p>
                </div>
                
                <!-- Mensagem de Erro -->
                <% if (locals.error) { %>
                    <div class="mb-6 p-4 rounded-lg bg-red-50 text-red-700 border border-red-200">
                        <div class="flex items-center">
                            <svg class="w-5 h-5 mr-2" fill="currentColor" viewBox="0 0 20 20">
                                <path fill-rule="evenodd" d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7 4a1 1 0 11-2 0 1 1 0 012 0zm-1-9a1 1 0 00-1 1v4a1 1 0 102 0V6a1 1 0 00-1-1z" clip-rule="evenodd"></path>
                            </svg>
                            <span><%= error %></span>
                        </div>
                    </div>
                <% } %>
                
                <!-- Formulário -->
                <form action="/mfa-verify" method="POST" class="space-y-6">
                    
                    <!-- Código TOTP -->
                    <div>
                        <label for="token" class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
                            Código do Aplicativo Autenticador
                        </label>
                        <input 
                            type="text" 
                            id="token" 
                            name="token" 
                            required 
                            maxlength="8"
                            autocomplete="off"
                            class="w-full px-4 py-4 border border-gray-300 dark:border-gray-600 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent dark:bg-gray-700 dark:text-white text-center text-2xl font-mono tracking-widest"
                            placeholder="000000"
                        >
                        <p class="text-xs text-gray-500 dark:text-gray-400 mt-2 text-center">
                            Digite o código de 6 dígitos ou código de backup de 8 dígitos
                        </p>
                    </div>
                    
                    <button 
                        type="submit"
                        class="w-full bg-gradient-to-r from-blue-500 to-indigo-600 text-white py-4 px-6 rounded-lg font-bold text-lg hover:from-blue-600 hover:to-indigo-700 transform transition duration-200 hover:scale-105 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 shadow-lg"
                    >
                        <span class="flex items-center justify-center">
                            <svg class="w-6 h-6 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            🔐 Verificar e Entrar
                        </span>
                    </button>
                </form>
                
                <!-- Ajuda -->
                <div class="mt-8 p-4 bg-gray-50 dark:bg-gray-700 rounded-lg">
                    <h4 class="font-bold text-gray-800 dark:text-gray-200 mb-2 text-sm">
                        💡 Precisa de ajuda?
                    </h4>
                    <ul class="text-gray-600 dark:text-gray-400 text-xs space-y-1">
                        <li>• Abra seu aplicativo autenticador (Google Authenticator, Authy, etc.)</li>
                        <li>• Procure por "Meu Portfólio" na lista</li>
                        <li>• Digite o código de 6 dígitos exibido</li>
                        <li>• Se não tiver acesso ao app, use um código de backup</li>
                    </ul>
                </div>
                
                <!-- Código de Backup -->
                <div class="mt-6 text-center">
                    <button 
                        onclick="toggleBackupHelp()"
                        class="text-sm text-blue-600 dark:text-blue-400 hover:text-blue-800 dark:hover:text-blue-300 font-medium"
                    >
                        🔑 Usar código de backup de emergência
                    </button>
                    
                    <div id="backup-help" class="hidden mt-4 p-4 bg-yellow-50 dark:bg-yellow-900/20 border border-yellow-200 dark:border-yellow-700 rounded-lg">
                        <h5 class="font-bold text-yellow-800 dark:text-yellow-200 mb-2 text-sm">
                            📋 Códigos de Backup
                        </h5>
                        <p class="text-yellow-700 dark:text-yellow-300 text-xs">
                            Se você perdeu acesso ao seu aplicativo autenticador, pode usar um dos códigos 
                            de backup de 8 dígitos que foram fornecidos durante a configuração inicial.
                            Digite o código no campo acima.
                        </p>
                    </div>
                </div>
                
            </div>
            
            <!-- Footer -->
            <div class="px-8 py-4 bg-gray-50 dark:bg-gray-700 border-t dark:border-gray-600 text-center">
                <p class="text-sm text-gray-600 dark:text-gray-400">
                    <a href="/login.html" class="text-blue-600 dark:text-blue-400 hover:text-blue-700 dark:hover:text-blue-300 font-medium">
                        ← Voltar ao Login
                    </a>
                </p>
            </div>
        </div>
        
    </div>
    
    <script>
        function toggleBackupHelp() {
            const helpDiv = document.getElementById('backup-help');
            helpDiv.classList.toggle('hidden');
        }
        
        // Auto-format do código
        document.getElementById('token').addEventListener('input', function(e) {
            let value = e.target.value.replace(/\D/g, '');
            if (value.length > 8) value = value.substring(0, 8);
            e.target.value = value;
        });
        
        // Focus automático
        document.getElementById('token').focus();
    </script>
    
    <script src="/js/theme-toggle.js"></script>
</body>
</html>
